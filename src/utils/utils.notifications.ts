// import { notify } from "store/store";

export const generateNotification = async (
  title: string,
  message: string,
  link?: string
) => {
  // const id = await notify.notify.create({
  //   title,
  //   message,
  //   isClickable: true,
  //   silent: false,
  // });
  // console.log("🚀 ~ file: utils.notifications.ts:14 ~ id:", id);
  // setTimeout(() => {
  //   notify.notify.clear(id);
  // }, 4000);
  // return id;
};
